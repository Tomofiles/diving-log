import React from 'react'

import {
  Typography
} from "@material-ui/core";

const MyMap = props => {
  return (
    <main className={props.classes.content}>
      <Typography variant="h1">MYMAP</Typography>
    </main>
  )
}

export default MyMap;