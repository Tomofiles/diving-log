import React from 'react'

import {
  Typography
} from "@material-ui/core";

const MyShop = props => {
  return (
    <main className={props.classes.content}>
      <Typography variant="h1">MYSHOP</Typography>
    </main>
  )
}

export default MyShop;